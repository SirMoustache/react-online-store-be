# import * from './generated/prisma.graphql'

type Mutation {
  createUser(name: String!, email: String!): User!
  createShopItem(
    title: String!
    preview: String!
    largePreview: String!
  ): ShopItem!
}

type Query {
  users: [User]!
  shopItems: [ShopItem]!
}
