# import * from './generated/prisma.graphql'

type Mutation {
  createUser(name: String!, email: String!): User!
  createShopItem(title: String!): ShopItem!
}

type Query {
  users: [User]!
  shopItems: [ShopItem]!
}
